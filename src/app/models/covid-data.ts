export interface CovidData {
    country : string,
    confirmed : number,
    recovered : number,
    death : number,
    active : number
}

export interface DateCovidData {
    date : Date,
    confirmed : number
}
